name: challenge

on: push

jobs:
    job1:
        name: ubuntu
        runs-on: ubuntu-latest
        steps:
            - name: Step one
              uses: actions/checkout@v2
            - name: Step two
              run: date 
